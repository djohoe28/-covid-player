{
  "name": "covid-player",
  "private": "true",
  "module": "index.ts",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "server:start": "bun run --filter @covid-player/server start",
    "clean": "rm -rf **/node_modules **/bun.lockb"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}